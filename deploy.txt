# Defaults for .github/workflows/deploy-hotair.yml.
#
# The workflow_dispatch form is pre-filled with these values when the
# repository is generated. If you edit this file later, update the workflow input
# defaults to match.

region=us-east-1
instance_type=m7i.xlarge
memory_gb=32
server_name=shared-canvas-debug
app_name=shared-canvas-debug
dns_host=shared-canvas-debug.example.com
ssh_key_name=shared-canvas-ec2
ssh_private_key_path=.deploy/ssh/shared-canvas-ec2.pem
